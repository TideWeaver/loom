[package]
name = "loom-cli"
version = "0.0.1"
edition = "2024"

[dependencies]
clap = {version = "4.5.38",  features = ["derive", "cargo"]}
snafu = {workspace = true}

[[bin]]
name = "loom"
path = "src/main.rs"
