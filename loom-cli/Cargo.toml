[package]
edition = "2024"
name = "loom-cli"
version = "0.0.1"

[dependencies]
clap = { version = "4.5.38", features = ["derive", "cargo"] }
loom = { path = "../loom" }
snafu = { workspace = true }

[[bin]]
name = "loom"
path = "src/main.rs"
